# Corpscout Translation Service configuration.
#
# Copy this file to .env for local development. In Docker/production, pass the
# same variables through docker compose or the process environment.

# Default provider/model used when a request passes provider=default/model=default.
TRANSLATION_DEFAULT_PROVIDER=local
TRANSLATION_DEFAULT_MODEL=qwen3:6b

# Provider-specific OpenAI-compatible endpoint.
TRANSLATION_PROVIDER_LOCAL_BASE_URL=http://100.77.62.33:8888
TRANSLATION_PROVIDER_LOCAL_MODEL=qwen3:6b

# Optional. Leave empty for local LLM endpoints that do not require a key.
TRANSLATION_PROVIDER_LOCAL_API_KEY=

# Requests can also provide llm.base_url / llm.api_key inline. The provider
# env variables are fallback values for requests that only pass llm.provider.
# Example provider slug deepseek-v4-flash:
# TRANSLATION_PROVIDER_DEEPSEEK_V4_FLASH_BASE_URL=https://api.deepseek.com
# TRANSLATION_PROVIDER_DEEPSEEK_V4_FLASH_MODEL=deepseek-v4-flash
# TRANSLATION_PROVIDER_DEEPSEEK_V4_FLASH_API_KEY=

# Shared decryption key for DB-backed LLM provider credentials received from
# Corpscout. Use the same value as CORPSCOUT_LLM_PROVIDER_KEY_ENCRYPTION_KEY.
# Use a stable passphrase, or use base64:<32-byte-key> from: openssl rand -base64 32
LLM_PROVIDER_KEY_ENCRYPTION_KEY=

# Request timeout for LLM calls.
TRANSLATION_LLM_TIMEOUT_SECONDS=120

# NATS worker mode.
NATS_URL=nats://localhost:4222
TRANSLATION_NATS_SUBJECT=brreg.translation.translate
TRANSLATION_NATS_QUEUE=brreg-translation
TRANSLATION_WORKER_MAX_CONCURRENT_REQUESTS=1
