<!--
SOURCE DOSSIER TEMPLATE — copy to <country>/<source>/dossier.md and fill in.
Keep it terse and evidence-grounded (tables over prose). Every claim should be
backed by the profile artifacts or a cited endpoint, not memory.
Delete the guidance comments (<!-- ... -->) as you complete each section.
Status lifecycle: discovery → mapping → review → implementing → live → retired.
-->

# <Country> <Source> — Source Dossier

| | |
|---|---|
| **Source id** | `<country>_<source>` <!-- matches dagster SOURCE_NAME --> |
| **Country** | `<country>` |
| **Date** | `<YYYY-MM-DD>` |
| **Author** | `<name>` |
| **Status** | `discovery` |
| **Archetype** | `<snapshot \| window \| TBD>` |

## 1. Identity & access

| Field | Value |
|---|---|
| Publisher | `<official body>` |
| Endpoint / download | `<url>` |
| Auth | `<none \| api-key \| oauth>` |
| Cost | `<free \| paid: …>` |
| Format(s) | `<json \| csv \| xml \| xbrl \| parquet>` |
| License | `<e.g. CC-BY-4.0>` — redistribution `<ok \| restricted>` |
| Freshness | `<daily \| weekly \| annual \| ad-hoc>` |
| Entity coverage | `<what's in / explicitly excluded>` |

<!-- Cite the source_inventory.md row this expands on. -->

## 2. Acquisition

| Field | Value |
|---|---|
| Fetch mechanism | `<paginated API \| bulk file \| changefeed \| scrape>` |
| Pagination / cursor | `<page size, total pages, cursor field>` |
| Full volume | `<N records / M bytes>` |
| Sample obtained | `<N records, how selected, when>` |
| Rate limits / etiquette | `<observed limits, required User-Agent>` |
| Incremental signal | `<lastModified field \| none → full snapshot>` |

## 3. File / entity inventory

<!-- One row per logical file or API entity in the sample. -->

| Entity | Grain (1 row =) | Rows in sample | Links to | Notes |
|---|---|---:|---|---|
| `<companies>` | one company | `<N>` | — | base entity |
| `<addresses>` | one address | `<N>` | `companies.<key>` | |
| `<financials>` | one statement/period | `<N>` | `companies.<key>` | |

## 4. Schema profile

Evidence: `./profile.md`, `./profile.json` (generated by `profile_source.py`).

<!-- Summarize the profile; don't paste the whole thing. Flag the columns that
matter: candidate keys, high-null, high-cardinality, ambiguous codes. -->

| Column | Type | Null % | Distinct | Role | Notes |
|---|---|---:|---:|---|---|
| `<business_id>` | `<str>` | `<0>` | `<=rows>` | **primary key** | |
| `<…>` | | | | `<key \| core \| extras \| drop>` | |

Ambiguous / undocumented fields (need confirmation against official code lists):

- `<field>` — `<observation, hypothesis>`

## 5. Keys & joins (within this source)

| Purpose | Key |
|---|---|
| Company identity (stable PK) | `<business_id>` |
| Join companies ↔ financials | `<business_id>` |
| Join companies ↔ addresses | `<…>` |
| Cross-source identity (for later entity resolution) | `<LEI \| VAT \| reg-no>` |

<!-- Cross-source ENTITY RESOLUTION is a separate downstream layer — note the
candidate match keys here, but do NOT attempt to resolve across sources during
onboarding. -->

## 6. Source-to-target mapping (→ canonical contract)

Targets are the 8-table canonical contract (`_canonical/canonical_schema.md`). A
source feeds the **per-(company, country) fact tables**; entity-level
`company`/`persons` rows are assembled later by the entity zone from these plus
other countries. Tick what this source supplies, then map each ticked table.

| Canonical table | Supplied? | Notes |
|---|---|---|
| `registrations` | `<yes/no>` | the company record itself |
| `financials` | `<yes/no>` | tall metric rows |
| `company_websites` | `<yes/no>` | registry field and/or discovered |
| `company_contacts` | `<yes/no>` | phone / email / social |
| `company_people` | `<yes/no>` | owners/officers — **personal data** |
| `company_relationships` | `<yes/no>` | corporate ownership |

### 6.1 `registrations` (core + extras)

`registration_uid = <country>:<registration_number>`. Each field → **core** column,
**extras**, or **drop**. Promote extras → core later via fill-rate (§8).

| Target column | Source path | Tier | Transform | Notes |
|---|---|---|---|---|
| `registration_number` | `<source.path>` | core | direct | national PK |
| `legal_name` | `<source.path>` | core | `<pick current>` | |
| `lifecycle_status` / `is_active` | `<source.path>` | core | `<rule>` | **derived — document it** |
| `legal_form_code` | `<source.path>` | core | `<map → ELF>` | |
| `activity_code` | `<source.path>` | core | `<map → NACE>` | `activity_scheme` = national set |
| `addr_*` | `<source.path>` | core | reshape | |
| `employee_count` | `<source.path>` | core | | per-registration headcount |
| `eu_id` / `lei` / `vat_number` | `<source.path>` | core | | match keys → §7 |
| `<country-specific>` | `<source.path>` | extras | verbatim | rare/uncertain |
| `<…>` | `<source.path>` | drop | — | `<why>` |

### 6.2 `financials` (tall)

One row per (company, country, period, metric). New metrics are new **rows**.

| metric_code | Source concept / column | Period basis | Currency | Notes |
|---|---|---|---|---|
| `revenue` | `<concept + dimension>` | `<duration \| instant>` | `<EUR>` | |
| `total_assets` | `<…>` | instant | | |

<!-- XBRL: the metric is usually (concept_qname, dimension member), not a bare
element name — see finland/prh_xbrl_schema_spike. -->

### 6.3 `company_websites`

| Field | Source path / rule | Notes |
|---|---|---|
| `url` | `<source.path>` | registry field, else `crawl_discovery`/`search` |
| `scope` | `<entity_main \| registration>` | per-registration sites = `registration` |
| `source_kind` | `<registry \| crawl_discovery \| search>` | |

### 6.4 `company_contacts`

| contact_type | Source path / rule | Notes |
|---|---|---|
| `<phone\|email\|fax\|social>` | `<source.path>` | many registries exclude email/phone → discovered |

### 6.5 `company_people` (owners/officers — PERSONAL DATA / GDPR)

Person owners → here; corporate owners → §6.6. Hold only name + LinkedIn.

| Field | Source path / rule | Notes |
|---|---|---|
| `person_raw_name` | `<source.path>` | as named by the registry |
| `role` | `<beneficial_owner\|director\|…>` | |
| `ownership_pct` | `<source.path>` | |
| `linkedin_url` | `<discovered?>` | usually not in registry |

<!-- Settle lawful basis / retention before populating — see canonical GDPR note. -->

### 6.6 `company_relationships` (corporate ownership)

| Field | Source path / rule | Notes |
|---|---|---|
| `to_raw_name` / `to_raw_identifier` | `<source.path>` | counterparty; may be `dangling` |
| `relationship_type` | `<owns\|controls\|parent_of\|…>` | |
| `ownership_pct` | `<source.path>` | |

## 7. Conformance inputs (match keys & authority)

What this source contributes to **cross-country entity resolution** and
**survivorship** when this country's records are fused with other sources.

| Match key | Present? | Coverage |
|---|---|---|
| LEI | `<yes/no>` | `<%>` |
| EUID / BRIS | `<yes/no>` | `<%>` |
| national registration_number | yes | PK |
| VAT number | `<yes/no>` | `<%>` |
| name + address (fuzzy fallback) | yes | — |

**Authority / survivorship:** for fields shared with other sources, is this the
**authoritative** registry (wins) or **secondary** (loses unless others absent)?
`<e.g. authoritative for registry fields; financials deferred to the XBRL source>`

## 8. Coverage & promotion notes

<!-- This section feeds the extras→core promotion decision. -->

| Field | Fill rate in sample | Decision | Rationale |
|---|---|---|---|
| `<field>` | `<%>` | core | prevalent or product-central |
| `<field>` | `<%>` | extras | rare now; revisit after more sources |

- Fields landing in **extras** for now: `<list>`
- Fields to **watch** for promotion once N sources seen: `<list>`

## 9. Ingestion & automation decision

| Field | Value | Rationale |
|---|---|---|
| Archetype | `<snapshot \| window \| new: …>` | `<full dump vs time-partitioned>` |
| Partitioning | `<none \| monthly by … \| …>` | |
| Cadence | `<annual \| weekly \| on-demand>` | matches publisher freshness |
| Trigger | `<schedule \| sensor \| manual>` | default **STOPPED** |
| Concurrency key | `<source_name \| source_name:clickhouse>` | |
| New archetype needed? | `<no \| yes — describe>` | only if it recurs across sources |

## 10. Dagster implementation plan

Asset chain (build and verify one layer at a time):

```text
remote <source>
  -> raw            (<fetch> → S3, faithful)            [bespoke per source]
  -> structured     (parse + map → Parquet in S3)       [bespoke per source]
  -> canonical load (companies + financials → ClickHouse)[SHARED]
  -> automation     (eager cascade, schedule/sensor)    [SHARED pattern]
```

| Layer | Reuse | Bespoke work |
|---|---|---|
| raw | archetype scaffold | the fetch/pagination logic |
| structured | streaming/manifest libs | the parser + the §6/§7 mapping |
| canonical | shared loader | none (just supply mapped rows) |

## 11. Data-quality checks (asset_checks)

| Check | Asserts |
|---|---|
| `<keys_unique>` | PK unique after dedup |
| `<financials_have_values>` | no metric rows with null value |
| `<row_count_sane>` | sample/full volume within expected band |

## 12. Open questions

- `<unresolved issue, what would resolve it>`

## 13. Recommendation

<!-- Go / no-go, chosen archetype, and the single biggest risk. Mirror the tone
of the schema_spike "Recommendation" section. -->

`<Proceed with … / Block on … >`
