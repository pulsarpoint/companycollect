# Hosts that run CT log drains. `ctlogs` resolves via your ~/.ssh/config
# (the same alias the Makefile used as SERVER). Add more hosts under the group
# to fan the same role out to a fleet.
[ctlog_hosts]
ctlogs

# Example of a second drain host:
# ctlogs2 ansible_host=100.85.212.200
