---
# Copy to secrets.yml (git-ignored) next to install.yml. Loaded via vars_files.
# Use URL-safe values (e.g. `openssl rand -hex 24`): the application password
# travels inside the nats:// connection URL.
nats_app_password: "change-me"
nats_sys_password: "change-me-too"
