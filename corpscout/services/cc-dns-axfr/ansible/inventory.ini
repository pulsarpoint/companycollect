[cc_dns_axfr]
hetzner01 ansible_host=hetzner01 ansible_user=root

# `hetzner01` resolves via Tailscale/ssh-config; override `ansible_host` with the IP
# 100.106.174.123 if name resolution isn't available to the control machine.
