# Public names use the on-host recursive cache, never the router or a forwarder.
# Tailscale's more-specific per-link routing domains retain priority over ~.
[Resolve]
DNS=
DNS=127.0.0.1
FallbackDNS=
Domains=~.
Cache=no
DNSStubListener=yes
