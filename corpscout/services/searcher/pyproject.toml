[project]
name = "searcher"
version = "0.1.0"
description = "Long-running CloakBrowser service with a localhost CDP endpoint"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1.8",
    "cloakbrowser>=0.5.7",
    "markdownify>=1.2.3",
]
