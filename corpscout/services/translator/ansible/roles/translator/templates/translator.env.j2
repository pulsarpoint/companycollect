# MANAGED BY ANSIBLE (roles/translator). The system manager reads this
# root-only file before dropping privileges to the translator account.
TRANSLATOR_CONFIG_FILE={{ translator_config_file | to_json(ensure_ascii=False) }}
TRANSLATOR_API_ADDR={{ translator_listen_address | to_json(ensure_ascii=False) }}
TEMPORAL_ADDRESS={{ translator_temporal_address | to_json(ensure_ascii=False) }}
CLICKHOUSE_PASSWORD={{ translator_clickhouse_password | to_json(ensure_ascii=False) }}
