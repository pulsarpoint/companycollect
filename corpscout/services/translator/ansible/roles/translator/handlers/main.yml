---
- name: Restart translator API
  ansible.builtin.systemd_service:
    name: "{{ translator_service_name }}"
    enabled: true
    state: restarted
    daemon_reload: true
  when: not ansible_check_mode
