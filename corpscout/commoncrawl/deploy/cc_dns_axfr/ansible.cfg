[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False
stdout_callback = default
result_format = yaml
# Reuse the existing external vault password file so this package can decrypt
# the preserved vault without changing or duplicating secrets.
vault_password_file = ~/.config/ansible/cc-dns-scan
[ssh_connection]
pipelining = True
