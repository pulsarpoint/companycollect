---
- name: Reload systemd (worker)
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart cc-dns-scan
  ansible.builtin.systemd_service:
    name: "{{ service_name }}"
    state: restarted
